import {Product} from "./product";

export class OrderClient {

  id:number;
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  productId: number;
  quantity: number;
  finalPrice: number;
}
